I created the ec2 instance and then i connected it 
Now i installed nginx and then create the index.html 
copied the html file into nginx html
I created the elastic ip and merge it with the instance 
Now created the DNS named with  "praveens" with the elastic ip 
with the help of certbot i got ssl certificate